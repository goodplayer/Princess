# bind address
bind=":9101"

# host protocol: http/https
protocol="http"

# session encrypt key
session_encrypt_key="princess.key"

# session key
session_key="princess"

# template path
template_path="./templates/*"

# static path
static_path="./statics"

[db]
# postgresql database connection string
db_conn_str="user=dev password=pw1 dbname=blog sslmode=disable host=localhost port=5432 connect_timeout=20"
db_min_count=5
db_max_count=10

[http]
enable=true
bind=":8081"

[site]
default_site_name="Moetang Tech Blog"

[db_obj]
# postgresql database connection string
db_conn_str="user=dev password=pw1 dbname=blog sslmode=disable host=localhost port=5432 connect_timeout=20"
db_min_count=3
db_max_count=3
